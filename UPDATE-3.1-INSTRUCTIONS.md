# FlowMeter Mission Manager 3.1

این نسخه اصلاحات زیر را دارد:
- حذف واقعی شهر، چاه و مأموریت از Supabase با ترتیب صحیح و بدون برگشت داده بعد از Refresh
- جلوگیری از برگشت رکوردهای حذف‌شده از LocalStorage
- اصلاح ذخیره و بازیابی عکس‌های غذا و سایر هزینه‌ها
- اصلاح پخش و دانلود ویس‌ها و ثبت MIME مناسب برای فایل صوتی
- مارکر نقشه به‌صورت SVG داخلی و بدون وابستگی به marker.png
- پشتیبانی از Back مرورگر/گوشی برای حرکت بین صفحات داخلی سایت
- جلوگیری از هم‌پوشانی آیکون حذف با وضعیت کارت‌ها

## یک کار لازم در Supabase
فایل `supabase-migration-v6.sql` را یک بار در Supabase > SQL Editor اجرا کنید.

سپس تمام فایل‌های این پروژه را روی شاخه `main` گیت‌هاب جایگزین کنید. `node_modules` را آپلود نکنید.
